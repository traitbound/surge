# Smoke walk 3 — phase 0 — 2026-08-25

**Verdict: NO-GO** · SHA `5d024a5` · server cwd deliberately outside the source checkout · real `claude -p` worker.

The actuator itself was proven for the first time: a real worker ran a materialization in a worktree outside the bound repo, appended 11 spans through the MCP tools and hooks, heartbeated its lease, committed a document, and was reaped. The NO-GO was **lifecycle hygiene**, not the central bet.

## Fix verification (from walk 2)

NEW-1 (plugin embedded + extracted at boot, `--plugin-dir` override, per-spawn verify, fail-loud) **VERIFIED**; NEW-2 (exit-0-with-no-spans → Failed) **VERIFIED**; F1/F2/F4/F5/F6 spot-checks PASS.

## Findings

| # | Sev | Finding | Fix |
|---|---|---|---|
| N1 | P1 | `dispatch_doc_run` propagated spawn failure with `?` after inserting the run and minting a token — leaked a permanently `running` run, an orphaned credential, no `dispatch.failed` audit | routed through `fail_dispatch` (`65e02b6`) |
| N2 | P1 | Lease TTL enforced only inside the per-run `monitor` task, which dies with the process. No boot reconciliation, no independent reaper, no graceful shutdown. A SIGKILL or Ctrl-C left a run `running`, a lease held, worktree residue, and the issue permanently un-dispatchable — recoverable only by sqlite surgery | `reconcile_orphans()` at boot + standing lease sweeper + SIGTERM drain (`65e02b6`); operator recovery via `POST /api/issues/{id}/retry` (`c1cec27`) |
| N3 | P2 | No span carries `node_id` or `duration_ms`; the two-node claim is unverifiable from the span tree | deferred to Phase 1; checklist line amended at walk 4 (S5) |
| N4 | P2 | Spans opened `running` are never closed — append-only MCP surface has no close tool | instructions fixed (`e883805`); supervisor resolves dangling spans at terminalization (`65e02b6`). A close-span tool was **declined**: it would widen the runtime token past INV-AUTH-1's five capabilities and let the supervised side rewrite what Surge recorded (INV-EXEC-3, INV-OBS-2) |
| N5 | P2 | Reap is `--force`; uncommitted work product is destroyed. Survival is worker-discretionary | open — decision recorded, not code |
| N6 | P2 | The not-eligible/already-leased refusal wrote a run with no span | fixed on the supervisor path (`65e02b6`); the runtime-claim sibling was missed and fixed at walk 4 (S3) |
| N7 | P3 | Human token can append spans to any run including terminal ones; runtime token is project-scoped, not run-scoped | open; low impact on a single-user loopback product |
| N8 | P3 | Seeded library bodies pointed at `integrations/claude-plugin/README.md`, absent from any bound repo (INV-DATA-6) | fixed (`e883805`) |
| N9 | P3 | Missing `\` continuation baked source indentation into an operator-facing message | fixed (`65e02b6`) |
| N10 | P3 | `latest_for_issue` had no caller and no test, making the coverage line false | test added, function kept as the phase-2 hash-mismatch read (`e883805`) |
| N11 | P3 | Unknown `/api/*` returned `200 text/html` via the SPA fallback | JSON 404s on both nested routers (`e883805`) |
| N12 | P3 | `instance_meta` never written | open; keep or delete deliberately |
| N13 | P3 | `run.finished`/`dispatch.failed` audit rows carried NULL `project_id` | fixed (`65e02b6`); `run.aborted` missed and fixed at walk 4 (S4) |
| N14 | P3 | `SupervisorConfig::default()` still held the cwd-relative `plugin_dir` that caused the NEW-1 P0 | `Default` impl removed entirely — re-arming it now fails to compile (`65e02b6`) |
