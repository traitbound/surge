# Smoke walk 6 (scoped re-walk) — phase 0 — 2026-08-28

**Verdict: NO-GO** · SHA `711d245` · server cwd outside the source checkout · real `claude -p` v2.1.243 · three real dispatched runs · walked cold from a fresh DB.

**Scoped, not a full walk.** Walk 5's two blockers (F1, F2) plus the headline loop and the S8 UI change were the entire brief; the full Done-when checklist was not re-walked. Scope chosen by the coordinator during the phase-0 close, on the grounds that walk 5's carried findings were already triaged as non-blocking.

Per-item: **F1 = GO** (holds under adversarial probe) · **F2 = NO-GO** (tool built and exercisable; the Done-when clause still overclaims) · **headline loop = GO**, with one new P2 attached · **S8 = GO, provisional** (bundle verified, page not eyeballed).

## Verified this walk

| Check | Result |
|---|---|
| F1 — runtime token run-scoping | PASS, adversarially. A live run-bound token lifted from the worker's process env was refused (403 + audit row) against another run, a terminal run, cross-run span injection, another issue's work order, heartbeat hijack and the walk-5 claim exploit. Refusals fire *after* successful lookup (unknown ids give 404), so it is authorization, not a lookup miss. `store::tokens::mint` now refuses to mint an unbound runtime credential at all — the walk-5 shape is unrepresentable, not merely unused |
| Token lifecycle | PASS — each token revoked at run end; the dead token 401s on every path including its own run |
| Headline loop, 3 real workers | PASS — worktree per lease on the task branch, real `claude -p` with all five env vars injected (INV-AUTH-4), 12 worker spans with role and status, real heartbeat span evidence (first walk to have it), worktrees reaped at lease end with task branches surviving (INV-EXEC-2) |
| Anonymous probes | PASS — every protected path 401 json, hashed-asset miss 404 text/plain (walk-4 S6 stays closed), **zero 5xx** |
| S8 — cost column | PASS provisional — the asset served by the running binary has 2 × `cost n/m`, zero `$0.000` |

## Blockers

| # | Sev | Finding | Fix target |
|---|---|---|---|
| R1 | P2 | **A run whose worker reports an error span terminalizes `succeeded` and flips the issue to `verified`.** `iss_C`: the only span describing the worker's own work says `error`, the task branch is empty (`docs/note_C.md` never created), and Surge recorded success + verified, then reaped the worktree so the evidence of non-work left disk too. Root cause located: `crates/server/src/supervisor.rs:790-800` — the NEW-2 "observability floor" derives status from span *count* and heartbeat presence via `unobserved()`, never from span *status*; `supervisor.rs:849` then maps `Succeeded → Verified` unconditionally. Span status is a Surge-observed fact of exactly the same kind as span count. This is the "success with nothing behind it" class NEW-2 was written to close, one field over, and it is silent | `supervisor.rs` terminalization + a worst-span-status store query |
| R2 | P2 | **F2 carried, fourth consecutive walk on the same sentence.** The remediation half-landed: `surge_fetch_work_order` genuinely exists (`mcp/server.mjs:23`), is in `tools/list`, reads only `$SURGE_ISSUE_ID`, is registered in every worktree's `.claude/mcp.json`, and a real worker called it (`sp_db49f0082ef7`) — first span evidence of a real fetch. But only **1 of 3** real workers called it, because `supervisor.rs:389-394` inlines the entire rendered work order into the prompt and then *suggests* the tool; a model handed the work order has no reason to fetch it. The mandate lives in the `implementer` **subagent** definition, which the headless top-level worker never reads. The clause is backed by a tool a worker *may* call | `phase.md:44` (say "available and exercisable") **or** `supervisor.rs:389-394` (carry a pointer + hashes, not the rendered body) |

## Carried, not blocking

| # | Sev | Finding | Where it went |
|---|---|---|---|
| R3 | P3 | `auth.runtime_refused_privileged` audit rows carry a NULL `project_id` while all 7 sibling `auth.runtime_refused_scope` rows populate it — the privileged refusal formats the actor *from* the project and then discards it. `crates/server/src/auth.rs:36-42`, `audit_refusal` hardcodes `None`. Same shape as walk-4 S4 | open — trivial |
| — | — | **F1 scope note, deliberately not filed as a defect:** the *unbound project* runtime token (`POST /api/projects/{id}/runtime-token`) remains project-scoped by design and can read any issue's work order in the project — documented at `runtime_api.rs:18-29`, mitigated by rotation + a 15-minute TTL. Recorded here in these words so a future walk does not re-discover it and think F1 regressed | accepted by design |
| N5, N12, B1, B2 | — | out of scope for this walk by the brief; unchanged from walk 5 | open |

## Notes on the walk itself

The walker did not trust `crates/server/tests/runtime_scope.rs` (written by the same task as the F1 fix) and instead extracted a genuine supervisor-minted token from the live worker's process table to probe with. That is the right posture and is why F1's GO here is worth more than the previous walk's test-suite green.

S8 is graded **provisional**: verified by inspecting the bundle served by the running binary and the source, not by rendering the page. The tooltip is unverified as reachable. Someone should eyeball the Observatory once before this is called closed.
