# Smoke walk — phase 0 — 2026-08-25

**Verdict: NO-GO** · SHA `ec69b6e` · macOS, debug binaries, fresh DB, cold session (UI checked by curl only — browser walk pending, F7).

Headline: the real `claude -p` worker fails at spawn 100% of the time — two independent P0 root causes (F1 relative worktree path resolved against two bases; F2 `--mcp-config` swallowing the following positional in Claude Code 2.1.243) with a third latent P0 queued behind them (F3 relative `SURGE_PLUGIN_DIR` unresolvable from a worktree). Everything around the actuator walks clean: claim flow, bind, compile + closed write list + gitignore discipline, refusal runs, runtime-token capability gap, hashed-only token storage, TS seam.

## Checklist verdicts

| Done-when line | Verdict |
|---|---|
| One binary · claim URL · cold open shows claim (INV-AUTH-5) | PASS (UI part provisional) |
| Bind writes surge.yaml only · compile writes closed list · materialization row+hash | PASS |
| Edit fixture → new hash · stale dispatch refused with refusal run | PARTIAL — refusal path verified (via forced `fresh=0`); no walkable edit surface (F5) |
| Dispatch → worktree → headless worker → MCP spans → reap | **FAIL** (F1, F2, F3) |
| Gitignore surge-managed block discipline (INV-DATA-7) | PASS |
| Kill→TTL reclaim · Abort at next tool call | PARTIAL — bookkeeping verified; live-worker halves blocked by F1/F2 |
| Runtime token on human endpoint rejected + audited (INV-AUTH-2) | PASS |
| Generated TS types, no hand-written duplicates | PASS |
| Store queries compile-checked + tested + commit broadcast | PARTIAL FAIL — no broadcast exists yet (F6); module-level tests thin |

## Findings

| # | Sev | Finding | Root cause (located) | Fix target |
|---|---|---|---|---|
| F1 | P0 | Relative `work_dir` resolves against server cwd in some code paths and against the repo in `git -C` — dispatch 500s unless cwd == repo, and the "working" case puts worktrees INSIDE the bound repo (INV-DATA-1/EXEC-2 violation) | `supervisor.rs:44` relative default; masked by e2e tests overriding with absolute tempdirs | supervisor.rs config absolutization + default-config e2e |
| F2 | P0 | `claude -p --mcp-config X <work-order>` mis-parses: `--mcp-config` is variadic and consumes the positional; worker exits 1 in ~1s, zero MCP spans ever | `supervisor.rs:37-42,:208` arg order; never validated against the real binary | reorder args (positional before flag), prompt text not bare path |
| F3 | P0 | `SURGE_PLUGIN_DIR=integrations/claude-plugin` (relative) cannot resolve from inside a worktree | `supervisor.rs:45`, no absolutization; plugin packaging story open | absolutize; note distribution question |
| F4 | P1 | Dispatch failure after lease claim leaks a running run + held lease + unreaped worktree; worker stderr discarded (`Stdio::null`) so the real error was unobservable (INV-ERR-1) | no cleanup guard between `claim_lease` and monitor spawn; `:210-212` | error-path guard + stderr tail into failure span |
| F5 | P1 | Checklist line "editing the fixture produces a new hash" has no walkable surface | phase.md:43 assumes an edit path phase 0 never shipped | amend checklist line |
| F6 | P1 | Checklist "commit broadcast asserted" overstates — no broadcast exists (lands with Phase 2 SSE per ADR-3); store module tests thin | phase.md:49 vs store/lib.rs:5 | amend checklist line; module tests tracked |
| F7 | P2 | UI verified by curl only | — | browser walk at re-walk |
| F8 | P3 | `instance_meta` table never written | reserved-table intent unconfirmed | note; keep |

## Triage (2026-08-25)

- **F1–F4 → fix-in-place, one supervisor fix wave** (all `crates/server/src/supervisor.rs` + tests; regression tests first). Real-`claude` arg shape validated manually before re-walk.
- **F5, F6 → checklist amendments** in this phase doc (honesty over coverage theatre); store module-test thinness noted as follow-up, indirect coverage via 7 server suites accepted for phase 0.
- **F7 → re-walk includes a real browser pass.**
- **F8 → keep** (reserved for claim/backup state); no action.

Re-walk required: cold, on the fresh SHA, after the fix wave.
