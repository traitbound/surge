# Area: docs

Product and planning docs under `docs/`. Currently only `docs/design.md` (V3 spec). L1–L5 doc chain (concept → architecture → specs → taskgraph) to be authored here; open concept questions live in design §23.

Append date+branch-stamped sections below. Never overwrite existing sections.
